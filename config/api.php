<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Rate limits (запросов в минуту)
    |--------------------------------------------------------------------------
    |
    | api    — общий лимит на все эндпоинты: по пользователю, для гостей по IP.
    | Остальные — дополнительные лимиты на отдельные операции поверх общего.
    |
    */

    'rate_limits' => [
        'api' => (int) env('API_RATE_LIMIT', 120),
        'guest' => (int) env('API_GUEST_RATE_LIMIT', 30),
        'login' => (int) env('API_LOGIN_RATE_LIMIT', 5),
        'register' => (int) env('API_REGISTER_RATE_LIMIT', 10),
        'invitations' => (int) env('API_INVITATIONS_RATE_LIMIT', 20),
        'exports' => (int) env('API_EXPORTS_RATE_LIMIT', 5),
    ],

    /*
    |--------------------------------------------------------------------------
    | Время жизни кэша (секунды)
    |--------------------------------------------------------------------------
    |
    | Кэш сбрасывается событиями моделей сразу при изменении данных,
    | TTL — страховка на случай изменений в обход моделей.
    |
    */

    'cache_ttl' => [
        'team_role' => (int) env('API_CACHE_TEAM_ROLE_TTL', 600),
        'project_stats' => (int) env('API_CACHE_PROJECT_STATS_TTL', 600),
    ],

    /*
    |--------------------------------------------------------------------------
    | Устаревшие версии API
    |--------------------------------------------------------------------------
    |
    | Эндпоинты с заменой в новой версии отдают заголовки Deprecation и Sunset
    | (middleware deprecated). deprecated_at — с какой даты версия устарела,
    | sunset_at — когда её планируется отключить.
    |
    */

    'deprecations' => [
        'v1' => [
            'deprecated_at' => env('API_V1_DEPRECATED_AT', '2026-09-24'),
            'sunset_at' => env('API_V1_SUNSET_AT', '2027-03-31'),
        ],
    ],

];
